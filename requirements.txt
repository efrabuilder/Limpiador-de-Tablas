pandas>=2.0
openpyxl>=3.1
sqlalchemy>=2.0
streamlit>=1.38
typer>=0.12
rich>=13.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
ipywidgets>=8.1
jupyter>=1.0
matplotlib>=3.8
nbformat>=5.10

 
# Driver de base de datos SQL: sqlalchemy por si solo NO incluye ninguno.
# SQLite no necesita driver aparte (ya viene con Python). Los tres de abajo
# vienen activos por defecto porque los 3 motores (PostgreSQL/MySQL/SQL
# Server) son opciones de primera clase en la interfaz de conexión SQL
# (tanto para importar como para exportar); si no usa alguno, puede
# comentarlo para instalar menos dependencias.
psycopg2-binary>=2.9   # PostgreSQL
pymysql>=1.1           # MySQL / MariaDB
pyodbc>=5.1            # SQL Server (además requiere el "ODBC Driver 17/18 for SQL Server" instalado en el sistema)
