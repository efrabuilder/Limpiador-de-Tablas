pandas>=2.0
openpyxl>=3.1
sqlalchemy>=2.0
streamlit>=1.38
typer>=0.12
rich>=13.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
ipywidgets>=8.1
jupyter>=1.0
matplotlib>=3.8
nbformat>=5.10
 
# Driver de base de datos SQL: descomente el que necesite según el motor
# elegido en "Conectar a una base de datos SQL" (sqlalchemy por si solo NO
# incluye ninguno). SQLite no necesita driver aparte (ya viene con Python).
# psycopg2-binary>=2.9   # PostgreSQL
# pymysql>=1.1           # MySQL / MariaDB
# pyodbc>=5.1            # SQL Server (además requiere el "ODBC Driver 17/18 for SQL Server" instalado en el sistema)
